# 4. 순서쌍의 개수

> 문제 설명
> 순서쌍이란 두 개의 숫자를 순서를 정하여 짝지어 나타낸 쌍으로 (a, b)로 표기합니다.  
> 자연수 n이 매개변수로 주어질 때 두 숫자의 곱이 n인 자연수 순서쌍의 개수를 return하도록 solution 함수를 완성해주세요.
>
> 제한사항
>
> - 1 ≤ n ≤ 1,000,000
>   입출력 예
>   n result
>   20 6
>   100 9
>
> 입출력 예 설명
>
> - n이 20 이므로 곱이 20인 순서쌍은 (1, 20), (2, 10), (4, 5), (5, 4), (10, 2), (20, 1) 이므로 6을 return합니다.
> - n이 100 이므로 곱이 100인 순서쌍은 (1, 100), (2, 50), (4, 25), (5, 20), (10, 10), (20, 5), (25, 4), (50, 2), (100, 1) 이므로 9를 return합니다.

## 💭 나의 답안 및 풀이

```js
function solution(n) {
  let result = [];
  for (let i = 1; i <= n; i++) {
    if (n % i == 0) {
      result.push(i);
    }
  }
  return result.length;
}
```

n이라는 정수를 시작값을 1로 설정하고 n까지 for문을 돌려 순회하게하였다.  
n을 i로 나누었을때 나머지가 남지 않는다면 i \* 어떤값=n 일테니 조건문에 true인 i를 빈배열인 result에 push 하였다.  
그리고 그 result의 길이(개수)를 반환하였다.

# ❗️

1. 문제 푼 과정을 설명할때 내가 '이것을, 얘를'이라고 설명한다고 팀원분이 알려주셨다.
   그래서 문제해설을 좀더 용어를 명확히 써보려고하는데 정말 어렵다..노력하자.

2. 자주쓰는 메서드만 사용할 줄 알지 다른 용도들을 잘 모른다. 문제를 풀때 모르는 메서드를 더 자세히 알아보자.
